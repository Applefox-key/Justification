import React from "react";

const HidenText = ({ text }) => {
  return (
    <div className="hiden-wrap">
      ? <div className="hiden-text">{text}</div>
    </div>
  );
};

export default HidenText;
